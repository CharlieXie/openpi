# Waypoint VLA evaluation config — LIBERO Object
# Usage: python -m openpi.waypoint.eval_libero --config configs/eval_waypoint_libero.yaml

robot_type: libero
task_suite: libero_object

# Model checkpoints (set these to your trained checkpoint directories)
vlm_checkpoint: checkpoints/waypoint_vlm_libero/30000
ae_checkpoint: checkpoints/waypoint_ae_libero/30000

# Data / normalization
dataset_statistics_path: /workspace/data/libero_object_no_noops/1.0.0

# Model dimensions
model_action_dim: 32
model_proprio_dim: 32
horizon_steps: 32
num_waypoints: 7
max_token_len: 256

# Model architecture
paligemma_variant: gemma_2b
action_expert_variant: gemma_300m

# Normalization
norm_type: q99

# Evaluation
num_trials_per_task: 3
num_steps_wait: 10
